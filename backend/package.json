{
  "name": "gymcontrol-backend",
  "version": "1.0.0",
  "description": "API REST de GymControl (Express + SQLite)",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "db:migrate": "node src/db/migrate.js",
    "db:seed": "node src/db/seed.js",
    "db:seed:admin": "node src/db/seedAdmin.js",
    "db:setup": "npm run db:migrate && npm run db:seed && npm run db:seed:admin",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "gymcontrol",
    "express",
    "api"
  ],
  "author": "Diego Merida",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
